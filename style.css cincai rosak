/* ===== CSS RESET & VARIABLES ===== */
:root {
    /* Color Palette */
    --color-primary: #1E3A8A;
    --color-primary-light: #2563EB;
    --color-secondary: #64748B;
    --color-success: #059669;
    --color-danger: #DC2626;
    --color-warning: #D4AF37;
    
    /* Grayscale */
    --color-white: #FFFFFF;
    --color-gray-50: #F8FAFC;
    --color-gray-100: #F1F5F9;
    --color-gray-200: #E2E8F0;
    --color-gray-300: #CBD5E1;
    --color-gray-400: #94A3B8;
    --color-gray-500: #64748B;
    --color-gray-600: #47""5569;
    --color-gray-700: #334155;
    --color-gray-800: #1E293B;
    --color-gray-900: #0F172A;
    
    /* Functional Colors */
    --color-bg-body: #f5f5f5;
    --color-bg-container: var(--color-white);
    --color-bg-header: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
    --color-bg-help: #f0f4ff;
    --color-bg-input: var(--color-white);
    --color-bg-success: #F0FDF4;
    --color-bg-error: #FEF2F2;
    --color-bg-warning: #FEF7CD;
    
    /* Typography */
    --font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    --font-family-monospace: 'Menlo', 'Monaco', 'Consolas', monospace;
    --font-size-base: 14px;
    --line-height-base: 1.6;
    
    /* Spacing */
    --spacing-xs: 4px;
    --spacing-sm: 8px;
    --spacing-md: 12px;
    --spacing-lg: 16px;
    --spacing-xl: 20px;
    --spacing-2xl: 24px;
    --spacing-3xl: 32px;
    
    /* Border Radius */
    --radius-sm: 4px;
    --radius-md: 6px;
    --radius-lg: 8px;
    --radius-full: 25px;
    
    /* Shadows */
    --shadow-sm: 0 2px 4px rgba(0, 0, 0, 0.15);
    --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.05);
    --shadow-lg: 0 4px 8px rgba(0, 0, 0, 0.2);
    --shadow-focus: 0 0 0 3px rgba(37, 99, 235, 0.1);
    
    /* Transitions */
    --transition-fast: 0.2s ease;
    --transition-normal: 0.3s ease;
}

/* ===== BASE STYLES ===== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    -webkit-tap-highlight-color: transparent;
}

html {
    font-size: var(--font-size-base);
}

body {
    font-family: var(--font-family);
    line-height: var(--line-height-base);
    color: var(--color-gray-700);
    background: var(--color-bg-body);
    padding: var(--spacing-xl);
}

/* ===== LAYOUT & CONTAINERS ===== */
.container {
    max-width: 1200px;
    margin: 0 auto;
    background: var(--color-bg-container);
    border: 1px solid var(--color-gray-200);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
}

/* ===== HEADER & TYPOGRAPHY ===== */
h1 {
    background: var(--color-bg-header);
    color: var(--color-white);
    padding: var(--spacing-lg);
    border-radius: var(--radius-lg) var(--radius-lg) 0 0;
    font-weight: 600;
    text-align: center;
    font-size: 1.125rem;
}

/* ===== HELP & TUTORIAL SECTION ===== */
.help-section {
    display: flex;
    gap: var(--spacing-lg);
    margin: var(--spacing-sm) 0;
    padding: var(--spacing-sm);
    background: var(--color-bg-help);
    border-radius: var(--radius-md);
    flex-wrap: wrap;
}

.help-toggle {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    padding: var(--spacing-sm) var(--spacing-lg);
    background: var(--color-white);
    border-radius: var(--radius-full);
    border: 1px solid var(--color-gray-300);
}

.help-toggle-label {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-primary);
    cursor: pointer;
}

.help-content {
    background: #f8f9fa;
    border: 1px solid #dee2e6;
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    margin: var(--spacing-sm) 0;
    font-size: 0.75rem;
    line-height: 1.5;
    display: none;
}

.help-content h3 {
    color: var(--color-primary);
    margin-bottom: var(--spacing-sm);
    border-bottom: 1px solid #dee2e6;
    padding-bottom: var(--spacing-xs);
}

.help-content ul {
    padding-left: var(--spacing-xl);
    margin-bottom: var(--spacing-sm);
}

.help-content li {
    margin-bottom: var(--spacing-xs);
}

/* ===== INPUT SECTION ===== */
.input-section {
    padding: var(--spacing-xl);
    background: var(--color-gray-50);
    border-bottom: 1px solid var(--color-gray-200);
}

.paste-hint {
    font-size: 0.6875rem;
    color: var(--color-gray-500);
    margin-bottom: var(--spacing-sm);
    font-style: italic;
}

textarea {
    width: 100%;
    height: 150px;
    font-family: var(--font-family-monospace);
    font-size: 0.75rem;
    padding: var(--spacing-sm);
    border: 2px solid var(--color-gray-200);
    border-radius: var(--radius-md);
    resize: vertical;
    background: var(--color-bg-input);
    transition: all var(--transition-fast);
}

textarea:focus {
    border-color: var(--color-primary-light);
    outline: none;
    box-shadow: var(--shadow-focus);
}

/* ===== BUTTONS ===== */
.button-group {
    display: flex;
    gap: var(--spacing-sm);
    margin-top: var(--spacing-sm);
    flex-wrap: wrap;
}

button {
    padding: var(--spacing-md) var(--spacing-xl);
    font-size: 0.875rem;
    font-weight: 600;
    border: none;
    border-radius: var(--radius-full);
    cursor: pointer;
    transition: all var(--transition-normal);
    box-shadow: var(--shadow-sm);
}

button:hover:not(:disabled) {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
}

button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

.btn-primary { background: var(--color-danger); color: var(--color-white); }
.btn-secondary { background: var(--color-secondary); color: var(--color-white); }
.btn-info { background: var(--color-primary); color: var(--color-white); }
.btn-success { background: var(--color-success); color: var(--color-white); }
.btn-small { padding: var(--spacing-sm) var(--spacing-md); font-size: 0.75rem; }

/* ===== ADVANCED SETTINGS ===== */
.advanced-settings {
    margin-top: var(--spacing-lg);
    padding: var(--spacing-lg);
    border: 2px solid var(--color-gray-300);
    border-radius: var(--radius-md);
    background: var(--color-gray-100);
    display: none;
}

.advanced-settings.open { display: block; }

.settings-header {
    color: var(--color-primary);
    font-weight: 600;
    font-size: 0.875rem;
    margin-bottom: var(--spacing-md);
    border-bottom: 1px solid var(--color-gray-200);
    padding-bottom: var(--spacing-sm);
}

/* ===== FORM CONTROLS ===== */
select, input[type="text"], input[type="number"], input[type="range"] {
    width: 100%;
    padding: var(--spacing-sm);
    border: 2px solid var(--color-gray-200);
    border-radius: var(--radius-md);
    font-size: 0.875rem;
    background: var(--color-white);
}

/* ===== TOGGLE SWITCH ===== */
.toggle-switch {
    position: relative;
    display: inline-block;
    width: 50px;
    height: 26px;
}

.toggle-switch input {
    opacity: 0;
    width: 0;
    height: 0;
}

.toggle-slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: var(--color-gray-300);
    transition: 0.3s;
    border-radius: 34px;
}

.toggle-slider:before {
    position: absolute;
    content: "";
    height: 18px;
    width: 18px;
    left: 4px;
    bottom: 4px;
    background-color: var(--color-white);
    transition: 0.3s;
    border-radius: 50%;
}

input:checked + .toggle-slider {
    background-color: var(--color-primary-light);
}

input:checked + .toggle-slider:before {
    transform: translateX(24px);
}

/* ===== CONTROL GROUPS ===== */
.control-group {
    margin-bottom: var(--spacing-sm);
}

.control-group label {
    font-size: 0.75rem;
    font-weight: 500;
    display: block;
    margin-bottom: var(--spacing-xs);
}

.font-size-selector, .table-width-selector {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-md);
}

.font-size-presets {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-sm);
}

.print-option {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-md);
    padding-bottom: var(--spacing-md);
    border-bottom: 1px solid var(--color-gray-100);
}

.print-option:last-of-type {
    border-bottom: none;
}

.option-description {
    font-size: 0.625rem;
    color: var(--color-gray-500);
    font-weight: 400;
    display: block;
    margin-top: 2px;
}

/* ===== OUTPUT SECTION ===== */
.output-section {
    padding: var(--spacing-xl);
}

.flight-info {
    background: var(--color-bg-warning);
    color: var(--color-primary);
    border: 2px solid var(--color-warning);
    border-radius: var(--radius-md);
    font-weight: 600;
    text-align: center;
    padding: var(--spacing-sm);
    margin-bottom: var(--spacing-sm);
}

.stats {
    background: var(--color-bg-help);
    border: 1px solid var(--color-primary-light);
    border-radius: var(--radius-sm);
    border-left: 4px solid var(--color-primary-light);
    font-size: 0.625rem;
    padding: var(--spacing-sm);
    margin-bottom: var(--spacing-sm);
    line-height: 1.5;
}

.side-stats {
    background: var(--color-gray-50);
    border: 1px solid var(--color-gray-300);
    border-radius: var(--radius-sm);
    border-left: 4px solid var(--color-secondary);
    font-size: 0.5625rem;
    padding: var(--spacing-xs);
    margin-bottom: var(--spacing-xs);
}

/* ===== ZONE SECTIONS ===== */
.zone-section {
    margin-bottom: var(--spacing-lg);
}

.zone-header {
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
    color: var(--color-white);
    font-weight: 600;
    border-radius: var(--radius-md) var(--radius-md) 0 0;
    text-align: center;
    font-size: 0.75rem;
    padding: var(--spacing-sm);
    margin-bottom: var(--spacing-xs);
}

/* ===== TABLES ===== */
.tables-container {
    display: flex;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-sm);
}

.table-wrapper {
    flex: 1;
    max-width: 50%;
}

.table-title {
    background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
    color: var(--color-white);
    font-weight: 600;
    border-radius: var(--radius-md) var(--radius-md) 0 0;
    text-align: center;
    font-size: 0.6875rem;
    padding: var(--spacing-xs);
}

table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.5625rem;
    border: 1px solid var(--color-gray-200);
    table-layout: fixed;
}

th, td {
    border: 1px solid var(--color-gray-200);
    padding: 3px 4px;
    text-align: left;
}

th {
    font-weight: bold;
    background: var(--color-gray-100);
    color: var(--color-primary);
    text-align: center;
}

tbody tr:nth-child(even) { background-color: var(--color-gray-50); }
tbody tr:nth-child(odd) { background-color: var(--color-white); }

/* Table column widths */
.narrowbody-table th:nth-child(1),
.narrowbody-table td:nth-child(1) { width: 12%; text-align: center; }
.narrowbody-table th:nth-child(2),
.narrowbody-table td:nth-child(2) { width: 12%; }
.narrowbody-table th:nth-child(3),
.narrowbody-table td:nth-child(3) { width: 48%; }
.narrowbody-table th:nth-child(4),
.narrowbody-table td:nth-child(4) { width: 12%; text-align: center; }
.narrowbody-table th:nth-child(5),
.narrowbody-table td:nth-child(5) { width: 16%; }

.widebody-table th:nth-child(1),
.widebody-table td:nth-child(1) { width: 12%; text-align: center; }
.widebody-table th:nth-child(2),
.widebody-table td:nth-child(2) { width: 12%; }
.widebody-table th:nth-child(3),
.widebody-table td:nth-child(3) { width: 48%; }
.widebody-table th:nth-child(4),
.widebody-table td:nth-child(4) { width: 12%; text-align: center; }
.widebody-table td:nth-child(5) { width: 16%; }

.class-divider {
    border-top: 2px solid var(--color-gray-200);
    font-weight: bold;
    text-align: center;
    background: var(--color-white) !important;
}

/* ===== SEAT CHART STYLES ===== */
.seat-chart-container {
    overflow-x: auto;
    margin-bottom: var(--spacing-lg);
}

.seat-chart {
    border-collapse: collapse;
    min-width: 100%;
    border: 2px solid #1E3A8A !important;
    transition: all var(--transition-normal);
}

.seat-chart th,
.seat-chart td {
    border: 1px solid var(--color-gray-200) !important;
    padding: 0;
    text-align: center;
    vertical-align: top;
    height: 35px;
    overflow: hidden;
    transition: all var(--transition-normal);
}

/* Seat chart headers - UPDATED WITH HEX VALUES */
.seat-chart th,
.seat-chart .row-header {
    background: #1E3A8A !important;
    color: #FFFFFF !important;
    font-weight: bold;
    text-align: center;
    border: 1px solid #1E3A8A !important;
    padding: 0;
}

.seat-chart .aisle {
    background: #1E3A8A !important;
    border-left: 2px solid #E2E8F0 !important;
    border-right: 2px solid #E2E8F0 !important;
}

/* Optimized column widths */
.seat-chart .row-header {
    width: 16px !important;
    min-width: 9px !important;
    max-width: 16px !important;
}

.seat-chart .aisle {
    width: 10px !important;
    min-width: 10px !important;
    max-width: 10px !important;
}

/* Business/Economy specific widths */
.seat-chart.narrowbody-business td:not(.row-header):not(.aisle) {
    width: 65px !important;
    min-width: 65px !important;
    max-width: 65px !important;
}

.seat-chart.narrowbody-economy td:not(.row-header):not(.aisle) {
    width: 50px !important;
    min-width: 50px !important;
    max-width: 50px !important;
}

.seat-chart.widebody td:not(.row-header):not(.aisle) {
    width: 55px !important;
    min-width: 55px !important;
    max-width: 55px !important;
}

/* Seat cell styling */
.seat-cell {
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 1px;
    line-height: 1.1;
    font-weight: bold;
    text-transform: uppercase;
}

/* Empty seat alignment */
.seat-empty .seat-cell {
    align-items: flex-start;
    justify-content: center;
    text-align: left;
    padding-left: 4px;
}

/* Business class height */
.seat-cell.business-class {
    justify-content: flex-start;
    padding-top: 2px;
    height: auto;
    min-height: 65px;
}

/* Seat text lines */
.seat-line1, .seat-line2, .seat-line3 {
    line-height: 1.1;
    margin: 0;
    padding: 0;
    font-weight: bold;
    color: #000000 !important;
}

.seat-line2, .seat-line3 {
    font-weight: bolder;
}

.seat-line3 {
    margin-top: 1px;
    min-height: 12px;
}

.seat-line4 {
    line-height: 1.1;
    margin: 0;
    padding: 0;
    height: 12px;
    border-top: 1px dotted #999;
    margin-top: 2px;
}

/* Seat occupancy states - UPDATED WITH HEX VALUES */
.seat-occupied { 
    background: #E0E0E0 !important; 
    border: 1px solid #000000 !important;
}
.seat-empty { 
    background: #FFFFFF !important; 
    color: #94A3B8;
    border: 1px solid #E2E8F0 !important;
}
.seat-business-regular { 
    background: #FFFFFF !important; 
    border: 1px solid #E2E8F0 !important;
}

/* Tier overrides - UPDATED WITH HEX VALUES */
.seat-tier-emer, .seat-tier-plat { 
    background: #D4D4D4 !important; 
    border: 1px solid #000000 !important;
}
.seat-tier-gold { 
    background: #FFFFFF !important; 
    border: 1px solid #E2E8F0 !important;
}

/* ===== MESSAGES & FEEDBACK ===== */
.error {
    color: var(--color-danger);
    background: var(--color-bg-error);
    border: 2px solid var(--color-danger);
    padding: var(--spacing-sm);
    margin: var(--spacing-sm) 0;
    border-radius: var(--radius-sm);
}

.success {
    color: var(--color-success);
    background: var(--color-bg-success);
    border: 2px solid var(--color-success);
    padding: var(--spacing-sm);
    margin: var(--spacing-sm) 0;
    border-radius: var(--radius-sm);
}

.zone-error {
    color: var(--color-danger);
    font-size: 0.6875rem;
    margin: var(--spacing-xs) 0;
    padding: var(--spacing-xs);
    border: 1px solid var(--color-danger);
    background: var(--color-bg-error);
    display: none;
    border-radius: var(--radius-sm);
}

.zone-error.show { display: block; }

/* ===== SPECIAL ELEMENTS ===== */
.infant-meal {
    font-size: 0.5rem;
    color: var(--color-gray-600);
    display: block;
    margin-top: 2px;
}

.upgrade-indicator,
.downgrade-indicator {
    font-size: 0.4375rem;
    font-weight: bold;
    display: inline-block;
    margin-left: 3px;
}

.upgrade-indicator { color: var(--color-success); }
.downgrade-indicator { color: var(--color-danger); }

/* ===== DUAL CHART STYLES ===== */
.dual-chart-toggle {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    padding: var(--spacing-sm) var(--spacing-md);
    background: linear-gradient(135deg, #9333EA, #A855F7);
    color: var(--color-white);
    border-radius: var(--radius-full);
    font-weight: 600;
    font-size: 0.75rem;
    cursor: pointer;
    border: none;
    margin-left: var(--spacing-sm);
}

.dual-chart-indicator {
    background: #FEF3C7;
    color: #92400E;
    padding: var(--spacing-sm) var(--spacing-md);
    border-radius: var(--radius-md);
    font-size: 0.6875rem;
    font-weight: 600;
    margin: var(--spacing-sm) 0;
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    border-left: 3px solid #F59E0B;
    display: none;
}

.dual-chart-indicator.active { display: flex; }

.dual-chart-instructions {
    background: #ECFDF5;
    color: #065F46;
    padding: var(--spacing-sm) var(--spacing-md);
    border-radius: var(--radius-md);
    font-size: 0.6875rem;
    margin: var(--spacing-sm) 0;
    border-left: 3px solid #10B981;
}

.dual-chart-steps {
    font-size: 0.625rem;
    color: var(--color-gray-600);
    margin-top: var(--spacing-xs);
    padding-left: var(--spacing-lg);
}

.dual-chart-steps li {
    margin-bottom: var(--spacing-xs);
}

/* ===== CUSTOM ZONES ===== */
.custom-zones {
    margin-top: var(--spacing-lg);
    padding: var(--spacing-lg);
    border: 1px dashed var(--color-gray-600);
    border-radius: var(--radius-sm);
    background: var(--color-gray-100);
    display: none;
}

.custom-zones.open { display: block; }

.zone-item {
    display: flex;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-sm);
    align-items: center;
    flex-wrap: wrap;
}

.zone-name { width: 120px; }
.zone-start, .zone-end { width: 70px; }

.zone-checkboxes {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-lg);
}

.zone-checkbox {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
}

/* ===== SIMPLE STEPS ===== */
.simple-steps {
    margin: var(--spacing-xl) 0;
}

.step-simple {
    display: flex;
    align-items: center;
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-md);
    padding: var(--spacing-sm);
    background: var(--color-gray-50);
    border-radius: var(--radius-md);
    border-left: 3px solid var(--color-success);
}

.step-icon {
    width: 35px;
    height: 35px;
    background: linear-gradient(135deg, var(--color-success), #34D399);
    color: var(--color-white);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    flex-shrink: 0;
}

.step-text {
    font-size: 0.75rem;
    line-height: 1.4;
}

.step-text strong {
    color: var(--color-primary);
}

/* ===== CRITICAL NOTE ===== */
.critical-note {
    background: #fff5f5;
    border: 2px solid #fed7d7;
    border-left: 4px solid var(--color-danger);
    padding: var(--spacing-md);
    border-radius: var(--radius-md);
    margin: var(--spacing-lg) 0;
    font-size: 0.75rem;
}

.critical-note strong {
    color: var(--color-danger);
}

/* ===== PLATFORM SUMMARY ===== */
.platform-summary {
    display: flex;
    gap: var(--spacing-sm);
    margin: var(--spacing-lg) 0;
    flex-wrap: wrap;
}

.platform-card.mini {
    flex: 1;
    min-width: 120px;
    padding: var(--spacing-md);
    background: var(--color-white);
    border-radius: var(--radius-lg);
    border-left: 3px solid var(--color-primary-light);
    border: 1px solid var(--color-gray-200);
}

.platform-card.mini h4 {
    margin: 0 0 var(--spacing-sm) 0;
    font-size: 0.75rem;
    color: var(--color-primary);
}

.platform-card.mini p {
    font-size: 0.625rem;
    line-height: 1.4;
    margin: 0;
    color: var(--color-gray-500);
}

/* ===== DYNAMIC FONT SIZES ===== */
.output-section[data-font-size="7"] table,
.output-section[data-font-size="7"] th,
.output-section[data-font-size="7"] td { font-size: 7px !important; }

.output-section[data-font-size="8"] table,
.output-section[data-font-size="8"] th,
.output-section[data-font-size="8"] td { font-size: 8px !important; }

.output-section[data-font-size="9"] table,
.output-section[data-font-size="9"] th,
.output-section[data-font-size="9"] td { font-size: 9px !important; }

.output-section[data-font-size="10"] table,
.output-section[data-font-size="10"] th,
.output-section[data-font-size="10"] td { font-size: 10px !important; }

.output-section[data-font-size="11"] table,
.output-section[data-font-size="11"] th,
.output-section[data-font-size="11"] td { font-size: 11px !important; }

.output-section[data-font-size="12"] table,
.output-section[data-font-size="12"] th,
.output-section[data-font-size="12"] td { font-size: 12px !important; }

.output-section[data-font-size="13"] table,
.output-section[data-font-size="13"] th,
.output-section[data-font-size="13"] td { font-size: 13px !important; }

.output-section[data-font-size="14"] table,
.output-section[data-font-size="14"] th,
.output-section[data-font-size="14"] td { font-size: 14px !important; }

/* Table widths */
.output-section[data-table-width="50"] { width: 50%; }
.output-section[data-table-width="60"] { width: 60%; }
.output-section[data-table-width="70"] { width: 70%; }
.output-section[data-table-width="80"] { width: 80%; }
.output-section[data-table-width="90"] { width: 90%; }
.output-section[data-table-width="100"] { width: 100%; }
.output-section[data-table-width="110"] { width: 110%; }
.output-section[data-table-width="120"] { width: 120%; }
.output-section[data-table-width="130"] { width: 130%; }
.output-section[data-table-width="140"] { width: 140%; }
.output-section[data-table-width="150"] { width: 150%; }

/* Seat chart font sizes */
.seat-chart[data-font-size="8"] .seat-line1 { font-size: 8px !important; }
.seat-chart[data-font-size="8"] .seat-line2,
.seat-chart[data-font-size="8"] .seat-line3,
.seat-chart[data-font-size="8"] .seat-line4 { font-size: 10px !important; }

.seat-chart[data-font-size="9"] .seat-line1 { font-size: 9px !important; }
.seat-chart[data-font-size="9"] .seat-line2,
.seat-chart[data-font-size="9"] .seat-line3,
.seat-chart[data-font-size="9"] .seat-line4 { font-size: 11px !important; }

.seat-chart[data-font-size="10"] .seat-line1 { font-size: 10px !important; }
.seat-chart[data-font-size="10"] .seat-line2,
.seat-chart[data-font-size="10"] .seat-line3,
.seat-chart[data-font-size="10"] .seat-line4 { font-size: 12px !important; }

.seat-chart[data-font-size="11"] .seat-line1 { font-size: 11px !important; }
.seat-chart[data-font-size="11"] .seat-line2,
.seat-chart[data-font-size="11"] .seat-line3,
.seat-chart[data-font-size="11"] .seat-line4 { font-size: 13px !important; }

.seat-chart[data-font-size="12"] .seat-line1 { font-size: 12px !important; }
.seat-chart[data-font-size="12"] .seat-line2,
.seat-chart[data-font-size="12"] .seat-line3,
.seat-chart[data-font-size="12"] .seat-line4 { font-size: 14px !important; }

.seat-chart[data-font-size="13"] .seat-line1 { font-size: 13px !important; }
.seat-chart[data-font-size="13"] .seat-line2,
.seat-chart[data-font-size="13"] .seat-line3,
.seat-chart[data-font-size="13"] .seat-line4 { font-size: 15px !important; }

.seat-chart[data-font-size="14"] .seat-line1 { font-size: 14px !important; }
.seat-chart[data-font-size="14"] .seat-line2,
.seat-chart[data-font-size="14"] .seat-line3,
.seat-chart[data-font-size="14"] .seat-line4 { font-size: 16px !important; }

.seat-chart[data-font-size="15"] .seat-line1,
.seat-chart[data-font-size="15"] .seat-line2,
.seat-chart[data-font-size="15"] .seat-line3,
.seat-chart[data-font-size="15"] .seat-line4 { font-size: 15px !important; }

.seat-chart[data-font-size="16"] .seat-line1,
.seat-chart[data-font-size="16"] .seat-line2,
.seat-chart[data-font-size="16"] .seat-line3,
.seat-chart[data-font-size="16"] .seat-line4 { font-size: 16px !important; }

/* ===== SEAT CHART CONTROLS ===== */
.seat-chart-controls {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-lg);
    align-items: center;
    margin-bottom: var(--spacing-sm);
    padding: var(--spacing-sm);
    background: var(--color-gray-50);
    border-radius: var(--radius-md);
}

.seat-chart-controls label {
    font-size: 0.75rem;
    font-weight: 500;
}

.magic-option {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    padding: var(--spacing-sm) var(--spacing-md);
    background: linear-gradient(135deg, #8B5CF6, #A78BFA);
    color: var(--color-white);
    border-radius: var(--radius-full);
    font-weight: 600;
    font-size: 0.75rem;
}

.magic-option .toggle-switch {
    width: 40px;
    height: 20px;
}

.magic-option .toggle-slider:before {
    height: 14px;
    width: 14px;
    left: 3px;
    bottom: 3px;
}

.magic-option input:checked + .toggle-slider:before {
    transform: translateX(20px);
}

.magic-description {
    font-size: 0.625rem;
    font-style: italic;
}

/* ===== PRINT STYLES - MINIMAL MARGINS EDGE-TO-EDGE ===== */
@media print {
    /* FORCE PRINTING OF BACKGROUND COLORS - CRITICAL FIX */
    * {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
        color-adjust: exact !important;
    }
    
    /* REMOVE ALL PAGE MARGINS FOR EDGE-TO-EDGE PRINTING */
    @page {
        margin: 3mm !important;
    }
    
    body {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
        color-adjust: exact !important;
        background: #FFFFFF !important;
        margin: 0 !important;
        padding: 0 !important;
        width: 100% !important;
    }
    
    /* Container should be full width with NO margins/padding */
    .container {
        margin: 0 !important;
        padding: 0 !important;
        width: 100% !important;
        max-width: 100% !important;
        border: none !important;
        box-shadow: none !important;
        border-radius: 0 !important;
    }
    
    /* Output section fills page */
    .output-section {
        margin: 0 !important;
        padding: 0 !important;
        width: 100% !important;
    }
    
    /* Hide interactive elements */
    .input-section,
    button,
    .help-section,
    .help-content,
    .seat-chart-controls,
    .dual-chart-toggle,
    .dual-chart-indicator,
    .dual-chart-instructions,
    .success,
    .no-print,
    .acknowledgments,
    h1 {
        display: none !important;
    }
    
    /* Show/hide unassigned tables */
    .zone-section.unassigned-print { display: block !important; }
    .zone-section.unassigned-no-print { display: none !important; }
    
    /* Seat chart print optimization - FILLS PAGE */
    .seat-chart-container {
        margin: 0 !important;
        padding: 0 !important;
        width: 100% !important;
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
    }
    
    .seat-chart {
        margin: 0 !important;
        padding: 0 !important;
        width: 100% !important;
        border: 2px solid #1E3A8A !important;
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
    }
    
    /* CRITICAL: Ensure colors print correctly with exact hex values */
    .seat-chart th,
    .seat-chart .row-header {
        background: #1E3A8A !important;
        color: #FFFFFF !important;
        font-weight: bold !important;
        text-align: center !important;
        border: 1px solid #1E3A8A !important;
        padding: 0 !important;
        height: 20px !important;
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
    }
    
    .seat-chart .aisle {
        background: #1E3A8A !important;
        border-left: 2px solid #E2E8F0 !important;
        border-right: 2px solid #E2E8F0 !important;
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
    }
    
    .seat-line1,
    .seat-line2,
    .seat-line3,
    .seat-line4 {
        color: #000000 !important;
        font-weight: bold !important;
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
    }
    
    .seat-line4 {
        border-top: 1px dotted #999999 !important;
        height: 10px !important;
    }
    
    .seat-empty .seat-cell {
        align-items: flex-start !important;
        justify-content: center !important;
        text-align: left !important;
        padding-left: 4px !important;
    }
    
    .seat-cell.business-class {
        min-height: 60px !important;
        padding-top: 1px !important;
    }
    
    /* Seat colors - use exact HEX values */
    .seat-occupied {
        background: #E0E0E0 !important;
        border: 1px solid #000000 !important;
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
    }
    
    .seat-tier-emer,
    .seat-tier-plat {
        background: #D4D4D4 !important;
        border: 1px solid #000000 !important;
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
    }
    
    .seat-tier-gold,
    .seat-business-regular,
    .seat-empty {
        background: #FFFFFF !important;
        border: 1px solid #E2E8F0 !important;
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
    }
    
    /* Zone styling - MINIMAL MARGINS */
    .zone-section {
        margin: 0 !important;
        padding: 0 !important;
        page-break-inside: avoid !important;
    }
    
    .zone-section.page-break {
        page-break-before: always !important;
    }
    
    .zone-header {
        margin: 0 !important;
        padding: 1px !important;
        background: #1E3A8A !important;
        color: #FFFFFF !important;
    }
    
    .flight-info {
        background: #FEF7CD !important;
        color: #1E3A8A !important;
        border: 2px solid #D4AF37 !important;
        margin: 0 !important;
        padding: 1px !important;
    }
    
    .stats {
        background: #f0f4ff !important;
        border: 1px solid #2563EB !important;
        border-left: 4px solid #2563EB !important;
        margin: 0 !important;
        padding: 1px !important;
    }
    
    /* Compact print mode */
    body.compact-print-mode .stats {
        padding: 1px !important;
        font-size: 7px !important;
    }
    
    /* Port/Starboard split table widths - MINIMAL SPACING */
    .tables-container {
        display: flex !important;
        flex-direction: row !important;
        width: 100% !important;
        margin: 0 !important;
        padding: 0 !important;
        gap: 1mm !important; /* MINIMAL GAP */
    }
    
    .tables-container .table-wrapper {
        flex: 1 !important;
        max-width: calc(50% - 0.5mm) !important; /* MINIMAL CALCULATION */
        margin: 0 !important;
        padding: 0 !important;
    }
    
    .tables-container table {
        width: 100% !important;
        table-layout: fixed !important;
    }
    
    /* Table column widths */
    .tables-container .narrowbody-table th:nth-child(1),
    .tables-container .narrowbody-table td:nth-child(1) { width: 12% !important; }
    .tables-container .narrowbody-table th:nth-child(2),
    .tables-container .narrowbody-table td:nth-child(2) { width: 12% !important; }
    .tables-container .narrowbody-table th:nth-child(3),
    .tables-container .narrowbody-table td:nth-child(3) { width: 48% !important; }
    .tables-container .narrowbody-table th:nth-child(4),
    .tables-container .narrowbody-table td:nth-child(4) { width: 12% !important; }
    .tables-container .narrowbody-table th:nth-child(5),
    .tables-container .narrowbody-table td:nth-child(5) { width: 16% !important; }
    
    .tables-container .widebody-table th:nth-child(1),
    .tables-container .widebody-table td:nth-child(1) { width: 12% !important; }
    .tables-container .widebody-table th:nth-child(2),
    .tables-container .widebody-table td:nth-child(2) { width: 12% !important; }
    .tables-container .widebody-table th:nth-child(3),
    .tables-container .widebody-table td:nth-child(3) { width: 48% !important; }
    .tables-container .widebody-table th:nth-child(4),
    .tables-container .widebody-table td:nth-child(4) { width: 12% !important; }
    .tables-container .widebody-table td:nth-child(5) { width: 16% !important; }
    
    .table-title {
        background: #1E3A8A !important;
        color: #FFFFFF !important;
        margin: 0 !important;
        padding: 0 !important;
    }
    
    /* Ensure all table backgrounds print */
    th {
        background: #F1F5F9 !important;
        color: #1E3A8A !important;
    }
    
    tbody tr:nth-child(even) { 
        background-color: #F8FAFC !important;
    }
    
    tbody tr:nth-child(odd) { 
        background-color: #FFFFFF !important;
    }
    
    /* Tables in print - MINIMAL PADDING */
    th, td {
        padding: 1px 2px !important;
    }
    
    /* Side stats minimal */
    .side-stats {
        margin: 0 !important;
        padding: 0 !important;
    }
}

/* ===== UTILITY CLASSES ===== */
.no-print { display: none !important; }

.compact-print-mode .stats { 
    padding: 3px; 
    font-size: 8px; 
}

.avoid-orphan-rows tr { 
    page-break-inside: avoid; 
}

/* Landscape mode indicator */
body.landscape-mode::before {
    content: "";
    display: none;
}

/* Zone boundary */
.zone-boundary {
    border-bottom: 1px solid var(--color-primary) !important;
}

/* ===== ACKNOWLEDGMENTS ===== */
.acknowledgments {
    margin-top: var(--spacing-3xl);
    padding: var(--spacing-xl);
    background: var(--color-bg-header);
    color: var(--color-white);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
}

.acknowledgments h2 {
    text-align: center;
    margin-bottom: var(--spacing-lg);
    font-size: 1rem;
    border-bottom: 2px solid rgba(255, 255, 255, 0.3);
    padding-bottom: var(--spacing-sm);
}

.acknowledgments ul {
    margin-bottom: var(--spacing-lg);
}

.acknowledgments li {
    margin-bottom: var(--spacing-xs);
    font-size: 0.75rem;
    line-height: 1.4;
}

.acknowledgments a {
    color: #FEF3C7;
    text-decoration: none;
    font-weight: 600;
}

.acknowledgments a:hover {
    text-decoration: underline;
}

.acknowledgments .version-info {
    text-align: center;
    font-size: 0.625rem;
    opacity: 0.8;
    margin-top: var(--spacing-lg);
    line-height: 1.4;
}

/* ===== RESPONSIVE DESIGN ===== */
@media (max-width: 768px) {
    body {
        padding: var(--spacing-sm);
    }
    
    .button-group {
        flex-direction: column;
    }
    
    button {
        width: 100%;
        min-height: 44px;
    }
    
    .tables-container {
        flex-direction: column;
    }
    
    .table-wrapper {
        max-width: 100%;
    }
    
    .help-section {
        flex-direction: column;
    }
    
    .seat-chart-controls {
        flex-direction: column;
        align-items: flex-start;
    }
    
    .platform-summary,
    .simple-steps {
        flex-direction: column;
    }
    
    .platform-card.mini,
    .step-simple {
        width: 100%;
    }
    
    .step-simple {
        gap: var(--spacing-sm);
    }
    
    .step-icon {
        width: 30px;
        height: 30px;
        font-size: 0.875rem;
    }
    
    /* ===== IFRAME FULL-WIDTH FIX - HIGHEST PRIORITY ===== */
    .help-content {
        max-width: none !important;
        width: 100% !important;
    }
    
    .full-tutorial {
        width: 100% !important;
        max-width: none !important;
        margin: 20px 0 !important;
    }
    
    .iframe-container {
        position: relative !important;
        width: 100% !important;
        max-width: none !important;
        height: 600px !important;
        border: 1px solid #E5E7EB !important;
        border-radius: 8px !important;
        overflow: hidden !important;
        background: white !important;
        box-sizing: border-box !important;
    }
    
    .iframe-container iframe {
        width: 100% !important;
        height: 100% !important;
        border: none !important;
        display: block !important;
    }
    
    #iframe-loading,
    #iframe-loading-version {
        position: absolute !important;
        top: 0 !important;
        left: 0 !important;
        width: 100% !important;
        height: 100% !important;
        display: flex !important;
        flex-direction: column !important;
        justify-content: center !important;
        align-items: center !important;
        background: rgba(255, 255, 255, 0.95) !important;
        z-index: 10 !important;
    }
/* ============================================
   IMPROVED SAVED REPORTS - PROPER SPACING
   ============================================ */

.saved-reports-modern {
    margin: 30px 0;
    background: #FFFFFF;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    border: 1px solid #E2E8F0;
    overflow: hidden;
}

/* Header */
.saved-reports-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 24px;
    background: #F8FAFC;
    border-bottom: 1px solid #E2E8F0;
}

.header-left {
    display: flex;
    align-items: center;
    gap: 15px;
}

.header-icon {
    font-size: 24px;
}

.saved-reports-header h2 {
    margin: 0;
    font-size: 20px;
    font-weight: 600;
    color: #1E293B;
}

.count-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 28px;
    height: 28px;
    padding: 0 10px;
    background: #3B82F6;
    color: white;
    border-radius: 14px;
    font-size: 14px;
    font-weight: 600;
}

.close-button {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border: none;
    background: #E2E8F0;
    border-radius: 8px;
    color: #64748B;
    cursor: pointer;
    transition: all 0.2s ease;
}

.close-button:hover {
    background: #CBD5E1;
    color: #1E293B;
}

/* List Container */
.saved-reports-list {
    padding: 20px;
    max-height: 600px;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: 16px;
}

.saved-reports-list::-webkit-scrollbar {
    width: 10px;
}

.saved-reports-list::-webkit-scrollbar-track {
    background: #F1F5F9;
}

.saved-reports-list::-webkit-scrollbar-thumb {
    background: #CBD5E1;
    border-radius: 10px;
}

.saved-reports-list::-webkit-scrollbar-thumb:hover {
    background: #94A3B8;
}

/* Individual Report Card */
.report-card {
    display: grid;
    grid-template-columns: 28px 1fr auto;
    gap: 20px;
    align-items: center;
    padding: 20px;
    background: #FFFFFF;
    border: 2px solid #E2E8F0;
    border-radius: 10px;
    transition: all 0.2s ease;
}

.report-card:hover {
    border-color: #CBD5E1;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}

.report-card.selected {
    background: #EFF6FF;
    border-color: #3B82F6;
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

/* Checkbox */
.report-checkbox {
    display: flex;
    align-items: center;
}

.report-checkbox input[type="checkbox"] {
    appearance: none;
    width: 24px;
    height: 24px;
    border: 2px solid #CBD5E1;
    border-radius: 6px;
    background: white;
    cursor: pointer;
    transition: all 0.2s ease;
    position: relative;
    flex-shrink: 0;
}

.report-checkbox input[type="checkbox"]:hover {
    border-color: #3B82F6;
}

.report-checkbox input[type="checkbox"]:checked {
    background: #3B82F6;
    border-color: #3B82F6;
}

.report-checkbox input[type="checkbox"]:checked::after {
    content: '✓';
    position: absolute;
    color: white;
    font-size: 16px;
    font-weight: bold;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
}

/* Report Info */
.report-info {
    display: flex;
    flex-direction: column;
    gap: 12px;
    min-width: 0;
}

.report-title {
    font-size: 16px;
    font-weight: 600;
    color: #1E293B;
    line-height: 1.4;
    margin: 0;
}

.report-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    font-size: 14px;
    color: #64748B;
    line-height: 1.5;
}

.meta-item {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    white-space: nowrap;
}

.meta-item svg {
    width: 16px;
    height: 16px;
    flex-shrink: 0;
}

/* Action Buttons */
.report-actions {
    display: flex;
    gap: 10px;
    flex-shrink: 0;
}

.action-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border: 2px solid transparent;
    border-radius: 8px;
    font-size: 18px;
    cursor: pointer;
    transition: all 0.2s ease;
}

.action-btn.settings {
    background: #F3F4F6;
    color: #6366F1;
}

.action-btn.settings:hover {
    background: #EEF2FF;
    border-color: #6366F1;
}

.action-btn.view {
    background: #F0F9FF;
    color: #0EA5E9;
}

.action-btn.view:hover {
    background: #E0F2FE;
    border-color: #0EA5E9;
}

.action-btn.delete {
    background: #FEF2F2;
    color: #EF4444;
}

.action-btn.delete:hover {
    background: #FEE2E2;
    border-color: #EF4444;
}

/* Footer */
.saved-reports-footer {
    display: flex;
    gap: 12px;
    padding: 20px 24px;
    background: #F8FAFC;
    border-top: 1px solid #E2E8F0;
}

.footer-btn {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 14px 24px;
    border: none;
    border-radius: 8px;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
}

.footer-btn svg {
    width: 18px;
    height: 18px;
}

.footer-btn.primary {
    background: #10B981;
    color: white;
}

.footer-btn.primary:hover {
    background: #059669;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
}

.footer-btn.danger {
    background: white;
    color: #EF4444;
    border: 2px solid #FEE2E2;
}

.footer-btn.danger:hover {
    background: #FEF2F2;
    border-color: #EF4444;
}

/* Empty State */
.empty-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 80px 20px;
    text-align: center;
}

.empty-state-icon {
    font-size: 72px;
    margin-bottom: 20px;
    opacity: 0.4;
}

.empty-state h3 {
    margin: 0 0 12px 0;
    font-size: 20px;
    font-weight: 600;
    color: #1E293B;
}

.empty-state p {
    margin: 0;
    font-size: 15px;
    color: #64748B;
    line-height: 1.6;
}

/* Mobile Responsive */
@media (max-width: 768px) {
    .report-card {
        grid-template-columns: 28px 1fr;
        gap: 16px;
    }
    
    .report-actions {
        grid-column: 1 / -1;
        justify-content: flex-end;
        padding-top: 12px;
        border-top: 1px solid #E2E8F0;
        margin-top: 8px;
    }
    
    .saved-reports-footer {
        flex-direction: column;
    }
    
    .report-meta {
        flex-direction: column;
        gap: 8px;
    }
}
}